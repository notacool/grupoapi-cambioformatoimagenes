name: Tests Unitarios

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.8', '3.9', '3.10', '3.11']

    steps:
    - name: Checkout código
      uses: actions/checkout@v5

    - name: Configurar Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Cache dependencias pip
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-${{ matrix.python-version }}-

    - name: Instalar dependencias
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Verificar sintaxis
      run: |
        python -m py_compile src/**/*.py
        python -m py_compile main.py

    - name: Verificar imports básicos
      run: |
        python -c "from src.converter import TIFFConverter; print('✅ TIFFConverter importado correctamente')"
        python -c "from src.config_manager import ConfigManager; print('✅ ConfigManager importado correctamente')"
        python -c "from src.file_processor import FileProcessor; print('✅ FileProcessor importado correctamente')"
        python -c "from src.converters.pdf_compressor import PDFCompressor; print('✅ PDFCompressor importado correctamente')"

    - name: Ejecutar tests básicos
      run: |
        if [ -f "test_basic.py" ]; then
          echo "Ejecutando test_basic.py..."
          python test_basic.py || echo "⚠️ test_basic.py falló pero continuando..."
        else
          echo "⚠️ test_basic.py no encontrado, saltando..."
        fi
      continue-on-error: true

    - name: Ejecutar tests de compresión
      run: |
        if [ -f "test_compression.py" ]; then
          echo "Ejecutando test_compression.py..."
          python test_compression.py || echo "⚠️ test_compression.py falló pero continuando..."
        else
          echo "⚠️ test_compression.py no encontrado, saltando..."
        fi
        
        if [ -f "test_pdf_compression.py" ]; then
          echo "Ejecutando test_pdf_compression.py..."
          python test_pdf_compression.py || echo "⚠️ test_pdf_compression.py falló pero continuando..."
        else
          echo "⚠️ test_pdf_compression.py no encontrado, saltando..."
        fi
      continue-on-error: true

    - name: Ejecutar tests (si existen)
      run: |
        if [ -f "test_converter.py" ]; then
          echo "Ejecutando test_converter.py..."
          python test_converter.py || echo "⚠️ test_converter.py falló pero continuando..."
        fi
        
        if [ -f "test_met_converter.py" ]; then
          echo "Ejecutando test_met_converter.py..."
          python test_met_converter.py || echo "⚠️ test_met_converter.py falló pero continuando..."
        fi
      continue-on-error: true

    - name: Verificar funcionalidad básica
      run: |
        python -c "
        from src.config_manager import ConfigManager
        config = ConfigManager()
        print('✅ ConfigManager funciona correctamente')
        "
